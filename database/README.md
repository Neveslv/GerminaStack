# database

Adaptadores de persistencia do monolito. Este pacote contem PostgreSQL, migracoes e queries; os contratos usados pelos handlers ficam em `domain/`, evitando que a camada HTTP conheca detalhes do banco.
